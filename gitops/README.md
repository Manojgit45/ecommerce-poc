# E-commerce GitOps repository

Copy `helm/` and `argocd/` into a separate GitHub repository. Set the application repository variable `GITOPS_REPOSITORY` to this repository's `owner/repository` name and grant its `GITHUB_PUSH_TOKEN` secret Contents read/write access here.